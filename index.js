import { getPosts } from "./postController.js";

console.log(getPosts())


// const { generateRandomNums, celciusToFarenheit } = require("./utils");

// console.log(`Reandom Number: ${generateRandomNums()}`);
// console.log(`Celcius to Farenheit Conversion: ${celciusToFarenheit(0)}`);
